let msg: String = "welcome to typescript";
let firstMsg: String = msg.charAt(0);
let restMsg: String = msg.substring(1);
let newMsg: String = firstMsg.toUpperCase() + restMsg.toLowerCase();
console.log(msg);
console.log(newMsg);

